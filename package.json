{
  "name": "apprendre-polonais",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Polski z Żubrem — apprendre le polonais",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "typecheck:preview": "tsc --noEmit --checkJs",
    "verify": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
