{
  "name": "apprendre-polonais",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Polski z Żubrem — apprendre le polonais",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run test && npm run build"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20.19"
  }
}
